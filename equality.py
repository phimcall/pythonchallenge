# -*- coding: utf-8 -*-

import doctest
import re

def isUpperCase(character):
    """ Check Upper Case
    >>> isUpperCase('a')
    False
    >>> isUpperCase('A')
    True
    """
    pattern = r'^[A-Z]'
    if re.match(pattern, character)

def judge(word):
    guard_count = 0
    for char in word:
        if guard_count >= 3:
            
        if isUpperCase(char):
            
        
